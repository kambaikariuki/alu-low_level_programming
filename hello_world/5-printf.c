#include <stdio.h>
/**
* main - entry of program
*
* Description: prints statemement to screen
*
* Return: Returns 0 on success
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
